# Nginx Reverse Proxy for Odoo — Platinum Tier Cloud VM
# SSL handled by Let's Encrypt (certbot --nginx)

upstream odoo {
    server 127.0.0.1:8069;
}

upstream odoochat {
    server 127.0.0.1:8072;
}

server {
    listen 80;
    server_name odoo.yourdomain.com;

    # Redirect HTTP to HTTPS (enabled after certbot)
    # return 301 https://$host$request_uri;

    # Temporary: proxy directly until SSL is configured
    location / {
        proxy_pass http://odoo;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# HTTPS server block — uncomment after running certbot
# server {
#     listen 443 ssl http2;
#     server_name odoo.yourdomain.com;
#
#     ssl_certificate /etc/letsencrypt/live/odoo.yourdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/odoo.yourdomain.com/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     # Odoo web
#     location / {
#         proxy_pass http://odoo;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#         proxy_redirect off;
#
#         # Timeouts
#         proxy_connect_timeout 720s;
#         proxy_send_timeout 720s;
#         proxy_read_timeout 720s;
#     }
#
#     # Odoo longpolling
#     location /longpolling {
#         proxy_pass http://odoochat;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#     }
#
#     # Static files caching
#     location ~* /web/static/ {
#         proxy_pass http://odoo;
#         proxy_cache_valid 200 60m;
#         expires 24h;
#     }
#
#     # Gzip
#     gzip on;
#     gzip_types text/css text/plain application/json application/javascript;
#
#     client_max_body_size 50m;
# }
